#Load packages and define paths of the functions' lesson

library(pacman)
p_load(gtools,tidyverse, rio)



#SET PATHS -----------------------------------------------------------

dir_lesson = "functions"
dir_data = file.path(dir_lesson, "data")
